<%- include("../layouts/header"); -%>

    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">Email</th>
                <th scope="col">Date</th>
            </tr>
        </thead>
        <tbody>
            <% monthlySubscriber.forEach((monthlydata)=>{ %>
                <tr>
                    <td>
                        <%=monthlydata.email %>
                    </td>
                    <td>
                        <%=monthlydata.date %>
                    </td>
                </tr>
                <%}) %>
        </tbody>
    </table>

    <%- include("../layouts/footer"); -%>