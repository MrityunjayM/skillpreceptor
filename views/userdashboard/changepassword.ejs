<%- include('../layouts/header') %>

    <div class="container-fluid bg-soft-secondary px-sm-9 py-3 pt-sm-6 pb-sm-9">
        <div class="row justify-content-sm-center">
            <div class="col-md-6">
                <h1 class="mb-4">Change Password</h1>
                <form action="/user/changepassword" method="post">
                    <div class="mb-3">
                        <input type="password" id="name" name="currentpassword" class="form-control rounded-0" required
                            placeholder="Current Password" />
                    </div>
                    <div class="mb-3">
                        <input type="password" id="name" name="password" class="form-control rounded-0" required
                            placeholder="New Password" />
                        <% if(typeof userData !="undefined" ){ %>
                            <% if(typeof userData.password !='undefined' && userData.password.length<8 ){ %>
                                <div id="p1">
                                    <p style="width:fit-content; margin-top: 2px; height: fit-content; color: red;">
                                        Password must be at least 8 characters</p>
                                </div>
                                <% } %>
                                    <% } %>
                    </div>
                    <div class="mb-3">
                        <input type="password" id="name" name="password2" class="form-control rounded-0" required
                            placeholder="Confirm Password" />
                        <% if(typeof userData !="undefined" ){ %>
                            <% if(typeof userData.password2 !='undefined' && match ){ %>
                                <div id="p2">
                                    <p id="p2"
                                        style="width:fit-content; margin-top: 2px; height: fit-content; color: red;">
                                        Passwords do not match
                                    </p>
                                </div>
                                <% } %>
                                    <% } %>
                    </div>
                    <button type="submit" class="btn btn-secondary">Change</button>
                    <a href="/user/dashboard" class="btn btn-outline-secondary">Go Back</a>
                </form>
            </div>
        </div>
    </div>

    <%- include('../layouts/footer') %>